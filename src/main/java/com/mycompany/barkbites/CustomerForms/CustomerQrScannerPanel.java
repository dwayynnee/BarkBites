package com.mycompany.barkbites.CustomerForms;

import javax.swing.JFrame;

public class CustomerQrScannerPanel extends JFrame {
    public CustomerQrScannerPanel() { super(); init(); }
    private void init() { setTitle("QR Scanner"); setSize(300,200); }
}
